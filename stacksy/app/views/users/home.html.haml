#main_content
  #news_feed

    -@users.each do |user|
      =image_tag(user.profile.image_1)
      = user.name
      = user.age
      = user.gender.title
      = user.location
      
      %br



  / .popular tags

  / .local tech events
